<UserControl x:Class="RcloneQBController.Views.QbittorrentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RcloneQBController.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="#2D2D30">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <TextBlock Text="qBittorrent Connection" Foreground="#F1F1F1" FontSize="16" Margin="0,0,0,10" HorizontalAlignment="Center"/>
            <Button Content="Find my VPN IP" Command="{Binding FindVpnIpCommand}" Width="150" Margin="0,5" HorizontalAlignment="Center" Background="#007ACC" Foreground="#F1F1F1" BorderThickness="0"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Host:" Foreground="#F1F1F1" Margin="0,5"/>
                <TextBlock Style="{StaticResource InfoIconStyle}">
                    <TextBlock.ToolTip>
                        <ToolTip>This is the IP address of the device running qBittorrent. If you're using a VPN, this should be the VPN's IP address. You can use the 'Find my VPN IP' button to help you find it.</ToolTip>
                    </TextBlock.ToolTip>
                </TextBlock>
            </StackPanel>
            <TextBox Text="{Binding Host, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Background="#3E3E42" Foreground="#F1F1F1" BorderThickness="0"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Port:" Foreground="#F1F1F1" Margin="0,5"/>
                <TextBlock Style="{StaticResource InfoIconStyle}">
                    <TextBlock.ToolTip>
                        <ToolTip>This is the port number that qBittorrent is using for its web interface. The default is usually 8080. You can find this in qBittorrent's Web UI settings.</ToolTip>
                    </TextBlock.ToolTip>
                </TextBlock>
            </StackPanel>
            <TextBox Text="{Binding Port, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Background="#3E3E42" Foreground="#F1F1F1" BorderThickness="0"/>
            <TextBlock Text="Username:" Foreground="#F1F1F1" Margin="0,5"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Background="#3E3E42" Foreground="#F1F1F1" BorderThickness="0"/>
            <TextBlock Text="Password:" Foreground="#F1F1F1" Margin="0,5"/>
            <PasswordBox Name="PasswordBox" Background="#3E3E42" Foreground="#F1F1F1" BorderThickness="0"/>
            <Button Content="Test Connection" Command="{Binding TestConnectionCommand}" Width="150" Margin="0,15,0,0" HorizontalAlignment="Center" Background="#007ACC" Foreground="#F1F1F1" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</UserControl>