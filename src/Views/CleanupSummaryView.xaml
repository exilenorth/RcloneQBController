<UserControl x:Class="RcloneQBController.Views.CleanupSummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RcloneQBController.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="#2D2D30">
        <StackPanel Margin="15">
            <TextBlock Text="Final Summary & Test" Foreground="#F1F1F1" FontSize="16" Margin="0,0,0,10" HorizontalAlignment="Center"/>
            <TextBlock Text="Please review all your settings before finishing the setup." TextWrapping="Wrap" Foreground="#F1F1F1" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding ConfigurationSummary}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Background="#3E3E42" Foreground="#F1F1F1" BorderBrush="#3E3E42">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Setting" Binding="{Binding Key}" Width="*"/>
                    <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
            <Button Content="Run Final Test" Command="{Binding FinalTestCommand}" Width="150" Margin="0,15,0,0" HorizontalAlignment="Center" Background="#388E3C" Foreground="#F1F1F1" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</UserControl>