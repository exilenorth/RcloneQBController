<UserControl x:Class="RcloneQBController.Views.QbittorrentControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Margin="5">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Content" Value="Run Cleanup Script" />
                        <Setter Property="Command" Value="{Binding RunCleanupScriptCommand}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                <Setter Property="Content" Value="Stop" />
                                <Setter Property="Command" Value="{Binding StopCleanupScriptCommand}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <CheckBox Grid.Column="1" Content="Dry Run" IsChecked="{Binding IsDryRun}" VerticalAlignment="Center" Margin="5"/>
        </Grid>
    </Grid>
</UserControl>