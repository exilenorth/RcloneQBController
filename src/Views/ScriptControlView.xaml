<UserControl x:Class="RcloneQBController.Views.ScriptControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RcloneQBController.Views"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="300">
   <Grid>
       <StackPanel>
           <Expander Header="Rclone Jobs" IsExpanded="True">
               <ListView ItemsSource="{Binding Jobs}">
                   <ListView.ItemTemplate>
                       <DataTemplate>
                           <Grid>
                               <Grid.ColumnDefinitions>
                                   <ColumnDefinition Width="Auto" />
                                   <ColumnDefinition Width="*" />
                                   <ColumnDefinition Width="Auto" />
                                   <ColumnDefinition Width="Auto" />
                                   <ColumnDefinition Width="Auto" />
                               </Grid.ColumnDefinitions>
                               <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                                <Setter Property="Background" Value="LightBlue" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <ProgressBar Grid.Column="0" IsIndeterminate="True" Width="16" Height="16" Margin="0,0,5,0">
                                    <ProgressBar.Style>
                                        <Style TargetType="ProgressBar">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ProgressBar.Style>
                                </ProgressBar>
                                <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <Button Grid.Column="2" CommandParameter="{Binding}" Margin="0,0,5,0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Content" Value="Run Now" />
                                            <Setter Property="Command" Value="{Binding DataContext.RunJobCommand, RelativeSource={RelativeSource AncestorType=ListView}}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                                    <Setter Property="Content" Value="Stop" />
                                                    <Setter Property="Command" Value="{Binding DataContext.StopJobCommand, RelativeSource={RelativeSource AncestorType=ListView}}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Grid.Column="3" Content="Preview" Command="{Binding DataContext.PreviewCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}" Margin="0,0,5,0" />
                                <mah:ToggleSwitch Grid.Column="4" IsOn="{Binding IsScheduled}" Command="{Binding DataContext.ToggleScheduledJobCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Expander>
            <Expander Header="qBittorrent Tasks" IsExpanded="True">
                <local:QbittorrentControlView DataContext="{Binding Qbittorrent}" />
            </Expander>
        </StackPanel>
    </Grid>
</UserControl>